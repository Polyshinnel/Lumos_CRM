import { Text } from '@mantine/core';
import { PageContent } from '../components/layout/PageContent';

export function ClientsPage() {
    return (
        <PageContent title="Клиенты">
            <Text c="dimmed">Страница в разработке.</Text>
        </PageContent>
    );
}
